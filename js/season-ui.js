import {createSeason,loadSeason,simulateTeamGames,standings,conferenceStandings,playerSeasonStats,gameLog,nextTeamGame} from './season-engine.js?v=0.6.10';import {conferenceOf} from '../data/team-meta.js';
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const n=(x,d=1)=>Number(x||0).toFixed(d);const date=s=>s?new Date(`${s}T12:00:00Z`).toLocaleDateString('en-US',{month:'short',day:'numeric'}):'';
export async function renderSeasonHub(team){let s=loadSeason()||await createSeason();const root=document.querySelector('#seasonHub');if(!root)return;const draw=()=>{s=loadSeason()||s;const conf=conferenceOf(team),table=standings(s,conf),leaders=playerSeasonStats(s),logs=gameLog(s,team).slice(-3).reverse(),next=nextTeamGame(s,team),mine=table.find(x=>x.team===team),idx=Math.max(0,table.findIndex(x=>x.team===team)),snapshot=table.slice(Math.max(0,Math.min(idx-2,table.length-5)),Math.max(0,Math.min(idx-2,table.length-5))+5);root.innerHTML=`<div class="section">2026–27 NBA SEASON</div><div class="grid"><article class="card"><div class="label">YOUR RECORD</div><div class="metric">${mine?`${mine.w}–${mine.l}`:'0–0'}</div><div class="sub">${mine?`#${mine.rank} ${conf} • ${n(mine.pct*100)}%`:'Season ready'}</div></article><article class="card"><div class="label">YOUR NEXT GAME</div><div class="metric">${next?(next.home===team?`vs ${esc(next.away)}`:`@ ${esc(next.home)}`):'SEASON COMPLETE'}</div><div class="sub">${next?date(next.date):''} • ${gameLog(s,team).length} played</div></article></div><div class="seasonActions"><button class="btn" data-team-sim="1">SIM YOUR NEXT GAME</button><button class="btn" data-team-sim="10">SIM YOUR NEXT 10</button></div><button class="seasonPanel" data-season-view="standings"><div class="section">${conf.toUpperCase()} STANDINGS ›</div>${snapshot.map(x=>`<div class="seasonLine ${x.team===team?'on':''}"><b>${x.rank}. ${esc(x.team)}</b><span>${x.w}–${x.l}</span><span>${n(x.pct*100)}%</span></div>`).join('')}</button><button class="seasonPanel" data-season-view="leaders"><div class="section">LEAGUE LEADERS ›</div>${leaders.slice(0,3).map((p,i)=>`<div class="seasonLine"><b>${i+1}. ${esc(p.name)}</b><span>${n(p.ppg)} PPG</span></div>`).join('')||'<div class="empty">Simulate games to populate leaders.</div>'}</button><button class="seasonPanel" data-season-view="log"><div class="section">${esc(team)} RECENT GAMES ›</div>${logs.map(g=>{const us=g.home===team?g.homeScore:g.awayScore,them=g.home===team?g.awayScore:g.homeScore,opp=g.home===team?g.away:g.home;return `<div class="seasonLine"><b>${date(g.date)} • ${us>them?'W':'L'} ${us}–${them}</b><span>${g.home===team?'vs':'@'} ${esc(opp)}</span></div>`}).join('')||'<div class="empty">No games played yet.</div>'}</button>`;root.querySelectorAll('[data-team-sim]').forEach(b=>b.onclick=async()=>{root.querySelectorAll('button').forEach(x=>x.disabled=true);s=await simulateTeamGames(team,+b.dataset.teamSim);draw();document.dispatchEvent(new CustomEvent('seasonchange',{detail:{team}}))});root.querySelectorAll('[data-season-view]').forEach(b=>b.onclick=()=>renderSeasonDetail(root,b.dataset.seasonView,team,s,draw))};draw()}
function rows(a,team){return a.map(x=>`<div class="seasonLine ${x.team===team?'on':''}"><b>${x.rank}. ${esc(x.team)}</b><span>${x.w}–${x.l}</span><span>${n(x.pct*100)}%</span><span>${x.diff>=0?'+':''}${x.diff}</span></div>`).join('')}function renderSeasonDetail(root,view,team,s,back){const cs=conferenceStandings(s),leaders=playerSeasonStats(s),logs=gameLog(s,team).slice().reverse();let title='',body='';if(view==='standings'){title='NBA STANDINGS';body=`<div class="section">EASTERN CONFERENCE</div>${rows(cs.East,team)}<div class="section">WESTERN CONFERENCE</div>${rows(cs.West,team)}`}else if(view==='leaders'){title='LEAGUE LEADERS';body=leaders.slice(0,30).map((p,i)=>`<div class="seasonLine"><b>${i+1}. ${esc(p.name)}</b><span>${n(p.ppg)} PPG</span><span>${n(p.rpg)} RPG</span><span>${n(p.apg)} APG</span></div>`).join('')||'<div class="empty">No stats yet.</div>'}else{title=`${team} GAME LOG`;body=logs.map(g=>{const us=g.home===team?g.homeScore:g.awayScore,them=g.home===team?g.awayScore:g.homeScore,opp=g.home===team?g.away:g.home;return `<div class="seasonLine"><b>${date(g.date)} • ${us>them?'W':'L'} ${us}–${them}</b><span>${g.home===team?'vs':'@'} ${esc(opp)}</span><span>${g.overtime?`${g.overtime}OT`:''}</span></div>`}).join('')||'<div class="empty">No games played yet.</div>'}root.innerHTML=`<button class="back" id="seasonBack">‹ SEASON HOME</button><div class="section">${title}</div><div class="seasonDetail">${body}</div>`;root.querySelector('#seasonBack').onclick=back}
