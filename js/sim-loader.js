import {simulateGame} from './sim-engine.js?v=0.6.0';
const norm=s=>(s||'').normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]/gi,'').toLowerCase();let ratings=null;
async function data(){if(ratings)return ratings;const m=await import('../data/2k27-ratings.js?v=0.6.0');ratings=m.RATINGS_2K27;return ratings}
function enrich(rows,r){const idx=new Map(Object.entries(r).map(([n,v])=>[norm(n),v]));return rows.map(p=>{const x=r[p.name]||idx.get(norm(p.name));return x?{...p,rating2k:x.rating2k,ratings:x.ratings}:p})}
function table(rows){return `<div style="overflow:auto"><table style="width:100%;border-collapse:collapse;font-size:11px"><thead><tr><th>PLAYER</th><th>PTS</th><th>FG</th><th>3PT</th><th>FT</th><th>REB</th><th>AST</th><th>STL</th><th>BLK</th><th>TOV</th></tr></thead><tbody>${rows.sort((a,b)=>b.pts-a.pts).map(s=>`<tr><td>${s.name}</td><td>${s.pts}</td><td>${s.fgm}-${s.fga}</td><td>${s.tpm}-${s.tpa}</td><td>${s.ftm}-${s.fta}</td><td>${s.reb}</td><td>${s.ast}</td><td>${s.stl}</td><td>${s.blk}</td><td>${s.tov}</td></tr>`).join('')}</tbody></table></div>`}
window.NBAGMSim={async run(homePlayers,awayPlayers,homeName,awayName){const r=await data(),h=enrich(homePlayers,r),a=enrich(awayPlayers,r),g=simulateGame(h,a);return `<section class="hero"><div class="label">FINAL</div><h2>${homeName} ${g.score.home} — ${g.score.away} ${awayName}</h2><div class="sub">${g.possessions} possessions per team • possession-based NBA 2K27 simulation</div></section><div class="section">BOX SCORE</div>${table(g.box)}`}};
