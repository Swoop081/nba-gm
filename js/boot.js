const VERSION='0.6.8';
const versionEl=document.querySelector('[data-version]');if(versionEl)versionEl.textContent=`VERSION ${VERSION}`;
document.querySelector('#enterGame')?.addEventListener('click',()=>{document.querySelector('#launch')?.remove();document.body.classList.remove('launching')});
async function checkForUpdate(button){const original=button.dataset.label||button.textContent;button.dataset.label=original;button.disabled=true;button.textContent='CHECKING…';try{const u=new URL('./version.json',document.baseURI);u.searchParams.set('t',Date.now());const res=await fetch(u,{cache:'no-store'});if(!res.ok)throw new Error('Version check failed');const live=await res.json();if(!live?.version)throw new Error('Invalid version');if(live.version===VERSION){button.textContent=`UP TO DATE — v${VERSION}`;setTimeout(()=>{button.textContent=original;button.disabled=false},2200);return}const iu=new URL('./index.html',document.baseURI);iu.searchParams.set('t',Date.now());const ir=await fetch(iu,{cache:'no-store'});if(!ir.ok)throw new Error('New build is not ready');const html=await ir.text();if(!html.includes(`VERSION ${live.version}`)&&!html.includes(`v=${live.version}`))throw new Error('New build is still deploying');button.textContent=`UPDATE NOW — v${live.version}`;button.disabled=false;button.onclick=()=>{location.replace(`${new URL('./',document.baseURI).href}?v=${encodeURIComponent(live.version)}&t=${Date.now()}`)}}catch(e){button.textContent='CHECK FAILED — TAP TO RETRY';button.disabled=false;button.onclick=()=>checkForUpdate(button)}}
const updateButton=document.querySelector('#launchCheckUpdate');if(updateButton)updateButton.onclick=e=>checkForUpdate(e.currentTarget);
if('serviceWorker'in navigator){addEventListener('load',async()=>{try{const regs=await navigator.serviceWorker.getRegistrations();for(const r of regs)await r.unregister();const keys=await caches.keys();await Promise.all(keys.filter(k=>k.startsWith('nba-gm-')).map(k=>caches.delete(k)))}catch(e){console.warn('cache reset failed',e)}})}
